apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seafile-pvc
  namespace: seafile
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 10Gi
